# BRCA production P0001 coordinate execution approval

Direct user authorization is recorded for one P0001-only CPU coordinate
transaction: exactly one OpenSlide `read_region((0,0), 2, (5968,5120))`,
frozen segmentation, two coordinate bags, atomic no-overwrite publication,
validation, and the required stop.

The transaction uses zero-delete semantics. A runner-created staging directory
is atomically renamed on success and may remain for manual inspection on
failure; the runner contains no unlink or recursive-removal operation.

Patch reads, features, ResNet50, HEALNet, CUDA, P0002–P0008, prior pilots,
BLCA, Drive, raw-file deletion, cohort expansion, and training remain
prohibited.
