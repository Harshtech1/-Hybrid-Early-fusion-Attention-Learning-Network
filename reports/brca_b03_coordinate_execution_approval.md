# BRCA B03 coordinate execution approval

Direct user authorization is recorded for one B03-only CPU coordinate transaction: exactly one OpenSlide level-2 `read_region((0,0), 2, (5831,3632))`, frozen segmentation, two coordinate bags, atomic no-overwrite publication, validation, and the required stop.

The transaction uses zero-delete semantics. A runner-created staging directory is atomically renamed on success and may remain for manual inspection on failure; the runner contains no unlink or recursive-removal operation.

Patch reads, features, ResNet50, HEALNet, CUDA, other patients, Q25/Q50/Q75/B01/B02 or BLCA changes, Drive, deletion, cohort expansion, and training remain prohibited.
